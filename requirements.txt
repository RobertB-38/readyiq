# --- Core orchestration ---
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0

# --- Azure AI Foundry / Azure OpenAI ---
openai>=1.40.0
azure-identity>=1.17.0

# --- RAG / data ---
sec-edgar-downloader>=5.0.0
beautifulsoup4>=4.12.0
chromadb>=0.5.0          # local fallback if Foundry IQ unavailable during dev
tiktoken>=0.7.0

# --- Microsoft Learn MCP (live docs grounding) ---
mcp>=1.0.0

# --- API + UI ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
streamlit>=1.36.0
requests>=2.31.0

# --- Evaluation + observability ---
ragas>=0.2.0
python-dotenv>=1.0.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
